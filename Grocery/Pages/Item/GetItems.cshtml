@model System.Collections.Generic.List<Grocery.Domain.Models.Item>

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<h2>title</h2>
@foreach (var el in @Model)
{
    <p>@el.Id - @el.Name - @el.Price</p>
}
